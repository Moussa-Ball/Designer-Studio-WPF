<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mdi="clr-namespace:Studio.Softer.Windows.UI.MDI;assembly=Studio.Softer.Windows">
    
    <Style TargetType="{x:Type mdi:DocumentManagerSelector}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type  mdi:DocumentManagerSelector}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid x:Name="PART_LeftDock" Grid.Row="0"/>
                        <Grid x:Name="PART_Viewport" Grid.Row="1"/>
                        <Grid x:Name="PART_RightDock" Grid.Row="2"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
